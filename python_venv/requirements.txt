# ==============================================================================
# SenTient Python Dependencies (Falcon Layer)
# ==============================================================================
# Role: Runtime environment for the Semantic NLP Service.
# Strategy: Optimized for CPU inference (Torch CPU) to keep image size low.
# Usage: pip install -r requirements.txt
# ==============================================================================

# --- Core Web Framework ---
# Flask: Lightweight WSGI web application framework.
Flask==3.0.0
# Werkzeug: WSGI utility library for Flask.
Werkzeug==3.0.1
# Gunicorn: Production WSGI server (Workers managed via falcon_settings.yaml).
gunicorn==21.2.0

# --- NLP & Vectorization (The Heavy Lifting) ---
# Sentence-Transformers: Wrapper for SBERT models (all-MiniLM-L6-v2).
sentence-transformers==2.2.2
# PyTorch (CPU Only): Essential for running the Transformer models.
# Note: In production, use the specific index-url for CPU wheels to save space:
# pip install torch --index-url https://download.pytorch.org/whl/cpu
torch==2.1.0
# Transformers: Hugging Face core library.
transformers==4.35.0
# NumPy: Efficient matrix operations for vector math.
numpy==1.26.0

# --- Data Stores & Clients ---
# Elasticsearch: Client for connecting to the Context Store (Layer 2 DB).
elasticsearch==8.11.0
# PyYAML: For parsing 'config/nlp/falcon_settings.yaml'.
PyYAML==6.0.1

# --- Utilities ---
# Requests: For internal testing or health checks.
requests==2.31.0
# EditDistance: Fast implementation of Levenshtein (if moved to Python side).
editdistance==0.6.2
# Python-Dotenv: Loading environment variables (optional but good practice).
python-dotenv==1.0.0

# --- Dev / Testing ---
# PyTest: For running unit tests in 'evaluation/'.
pytest==7.4.3
# ==============================================================================
# SenTient Falcon Layer - Dependency Lockfile (RESOLVED)
# ==============================================================================
# Role: Ensures deterministic builds for the NLP container.
# Status: Fixes "cached_download" AND "tokenizers" conflicts.
# ==============================================================================

# --- Core Framework ---
Flask==3.0.0
gunicorn==21.2.0
requests==2.31.0

# --- NLP & Vectors (CPU Optimized) ---
torch==2.1.0 --index-url https://download.pytorch.org/whl/cpu

# LOCK: This specific triad must be kept in sync
transformers==4.34.0
tokenizers==0.14.1
huggingface-hub==0.17.3   # < 0.18 for tokenizers, but has cached_download for sbert

sentence-transformers==2.2.2
numpy==1.26.0
scikit-learn==1.3.2
scipy==1.11.3

# --- Data Stores ---
elasticsearch==8.11.0
redis==5.0.1

# --- Contracts & Config ---
PyYAML==6.0.1
jsonschema==4.20.0